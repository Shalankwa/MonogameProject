public enum ComponentType
{
    Sprite,
    PlayerInput,
    Animation,
	Collision,
	AIMovement,
	AnimationNPC,
	Camera,
	GUI,
	Damage,
	Stats
}

public enum Input
{
    Left,
    Right,
    Up,
    Down,
    None,
	Enter
}

public enum Direction
{
    Left,
    Right,
    Up,
    Down,
	NULL
}

public enum State
{
    Standing,
    Walking
}