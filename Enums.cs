public enum ComponentType
{
    Sprite,
    PlayerInput,
    Animation,
	Collision,
	AIMovement,
	AnimationNPC,
	Camera
}

public enum Input
{
    Left,
    Right,
    Up,
    Down,
    None
}

public enum Direction
{
    Left,
    Right,
    Up,
    Down,
	NULL
}

public enum State
{
    Standing,
    Walking
}